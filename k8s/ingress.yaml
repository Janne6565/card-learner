apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: card-learner
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - cards.example.com    # <-- replace with your domain
      secretName: card-learner-tls
  rules:
    - host: cards.example.com  # <-- replace with your domain
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: card-learner
                port:
                  number: 80
